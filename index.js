/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import Varam from './Varam';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Varam);
